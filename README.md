# task-manager-api

Task manager API built with Django and Django REST framework.

## Requirements

- Python 3.14
- uv

## Commands

```bash
uv sync
uv run python manage.py runserver
uv run ruff check .
uv run mypy .
```
